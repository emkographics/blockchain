createNewBlock(nonce, previousBlock, hash)
getLastBlock()
createNewTransaction(amount, sender, recepient)
addTransactionToPendingBlock(transaction)
proofOfWork(previousHashBlock, currentBlockData)
chainIsValid(blockchain)

/blockchain
/transaction
/transaction/broadcast 
/mine 

/recieve-new-block 
/register-and-broadcast-node 
/register-node 
/register-nodes-bulk 